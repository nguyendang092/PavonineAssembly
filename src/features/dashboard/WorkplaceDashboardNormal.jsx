import WorkplaceDashboard from "./workplace";

/** Route `/normal` — dashboard sản lượng (bar + upload NG → `ng/`). */
export default function WorkplaceDashboardNormal() {
  return <WorkplaceDashboard />;
}
